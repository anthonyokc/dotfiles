---
description: Show every writing change with exact classifications and counts
---

Load the `show-writing-diff` skill with the `skill` tool.

Use full mode.

Compare the original and revised writing in the current conversation or in files named by the user.

If one version is unavailable, identify the missing version. Then stop.

If the user requests an HTML comparison or interactive comparison, use HTML mode.

Apply these additional user instructions:

$ARGUMENTS
