---
description: Orchestrate difficult work with Fable 5.1 and optional Sol maximum reasoning.
agent: fable-orchestrator-max
subtask: false
---

Treat the following text as the complete user request.

Apply the routing and integration policy defined by the `fable-orchestrator-max` agent.

Use `sol-max` only when the configured maximum-effort conditions apply.

$ARGUMENTS
